apiVersion: batch/v1
kind: Job
metadata:
  name: achs-playwright-test
spec:
  template:
    spec:
      containers:
      - name: playwright
        image: achs-beneficios-tests:latest
        env:
        - name: BASE_URL
          value: "http://adonis-service:3000"
      restartPolicy: Never